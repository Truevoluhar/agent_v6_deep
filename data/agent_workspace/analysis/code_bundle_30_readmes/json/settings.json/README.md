# settings.json

- **Source file:** `/uploads/code_bundle_30/json/settings.json`
- **Language:** JSON
- **Purpose:** Stores simple application configuration values.
- **Key functions/classes:** No functions or classes; top-level object contains `appName`, `enabled`, and `retries` fields.
- **Inputs:** Consumed by any application or tooling that reads this configuration file.
- **Outputs:** Provides the app name `bundle-sample`, boolean enabled state `true`, and retry count `2`.
- **Notable implementation details:** The file is valid JSON with primitive string, boolean, and number values.
