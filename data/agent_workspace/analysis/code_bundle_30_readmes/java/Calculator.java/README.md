# Calculator.java

- **Source file:** `/uploads/code_bundle_30/java/Calculator.java`
- **Language:** Java
- **Purpose:** Defines a simple calculator utility for integer addition.
- **Key functions/classes:** Class `Calculator`; method `add(int a, int b)`.
- **Inputs:** Two `int` values, `a` and `b`.
- **Outputs:** Returns the integer sum `a + b`.
- **Notable implementation details:** The method is an instance method, not static; the class is in the default package and performs no overflow checks.
